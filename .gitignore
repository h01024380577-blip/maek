# ── 비밀·로컬 설정 ──
backend/.env
.env
*.local

# ── 제출 서류·문서 (개인정보 포함) ──
제출서류/
*.docx
*.doc
*.hwp
~$*

# ── 빌드·의존성·산출물 ──
node_modules/
frontend/dist/
backend/output/
# Vercel 빌드가 ../data 를 복사해 넣는 자리 (gitignore 는 줄 끝 주석을 허용하지 않는다)
backend/data/
__pycache__/
*.pyc
.venv/
venv/

# ── 도구·OS 찌꺼기 ──
.DS_Store
.playwright-mcp/
*.log
.ipynb_checkpoints/
